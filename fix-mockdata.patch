*** Begin Patch
*** Update File: src/data/mockData.ts
@@
-      if (!isAllZones) {
-         seed += zones.reduce((acc, z) => acc + z.id.split('').reduce((sum, char) => sum + char.charCodeAt(0), 0), 0), 0);
-      }
+      if (!isAllZones && Array.isArray(zones) && zones.length > 0) {
+        // safer, readable replacement to compute the sum of char codes of zone ids
+        const zoneCharsSum = zones.reduce((acc, z) => {
+          const idStr = String(z?.id ?? '');
+          const idSum = [...idStr].reduce((s, ch) => s + ch.charCodeAt(0), 0);
+          return acc + idSum;
+        }, 0);
+        seed += zoneCharsSum;
+      }
*** End Patch
